# 单域名部署 Nginx 配置模板
# 使用说明：
# 1. 将此配置复制到宝塔面板的网站配置文件中
# 2. 替换所有 "www.example.com" 为你的真实域名
# 3. 确认后端端口是 8080（如果不是，修改 proxy_pass）
# 4. 保存并重启 Nginx

server {
    listen 80;
    listen 443 ssl http2;
    server_name www.example.com;  # 👈 改为你的域名
    
    # SSL 证书配置（宝塔会自动添加，无需修改）
    # ssl_certificate /path/to/cert.pem;
    # ssl_certificate_key /path/to/key.pem;
    # ssl_protocols TLSv1.2 TLSv1.3;
    # ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:HIGH:!aNULL:!MD5:!RC4:!DHE;
    # ssl_prefer_server_ciphers on;
    # ssl_session_cache shared:SSL:10m;
    # ssl_session_timeout 10m;
    
    # 强制 HTTPS（可选，建议开启）
    # 如果需要强制 HTTPS，取消下面三行的注释
    # if ($server_port !~ 443){
    #     rewrite ^(/.*)$ https://$host$1 permanent;
    # }
    
    # 日志配置
    access_log /www/wwwlogs/buyticket_access.log;
    error_log /www/wwwlogs/buyticket_error.log;
    
    # 字符集
    charset utf-8;
    
    # 客户端上传大小限制（根据需要调整）
    client_max_body_size 50m;
    
    # ==========================================
    # 根目录 - Frontend-A (用户端)
    # ==========================================
    location / {
        root /www/wwwroot/buyticket/frontend-a;
        index index.html;
        try_files $uri $uri/ /index.html;
        
        # 静态资源缓存
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 7d;
            add_header Cache-Control "public, immutable";
        }
    }
    
    # ==========================================
    # 管理端 - Frontend-B
    # ==========================================
    location /admin {
        alias /www/wwwroot/buyticket/frontend-b;
        index index.html;
        try_files $uri $uri/ /admin/index.html;
        
        # 管理端静态资源
        location ~* ^/admin/.*\.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            alias /www/wwwroot/buyticket/frontend-b;
            expires 7d;
            add_header Cache-Control "public, immutable";
        }
    }
    
    # ==========================================
    # 核销端 - Frontend-C
    # ==========================================
    location /verify {
        alias /www/wwwroot/buyticket/frontend-c;
        index index.html;
        try_files $uri $uri/ /verify/index.html;
        
        # 核销端静态资源
        location ~* ^/verify/.*\.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            alias /www/wwwroot/buyticket/frontend-c;
            expires 7d;
            add_header Cache-Control "public, immutable";
        }
    }
    
    # ==========================================
    # 后端 API 代理
    # ==========================================
    location /api/ {
        proxy_pass http://127.0.0.1:8080;  # 👈 确认后端端口
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket 支持（如果需要）
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
        
        # 缓冲设置
        proxy_buffering on;
        proxy_buffer_size 4k;
        proxy_buffers 8 4k;
        proxy_busy_buffers_size 8k;
    }
    
    # ==========================================
    # 安全配置
    # ==========================================
    
    # 禁止访问隐藏文件
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }
    
    # 禁止访问备份文件
    location ~ ~$ {
        deny all;
        access_log off;
        log_not_found off;
    }
    
    # 禁止访问配置文件
    location ~* \.(sql|bak|swp|old|tmp)$ {
        deny all;
        access_log off;
        log_not_found off;
    }
    
    # ==========================================
    # 性能优化
    # ==========================================
    
    # Gzip 压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1k;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml text/javascript application/json application/javascript application/xml+rss application/rss+xml font/truetype font/opentype application/vnd.ms-fontobject image/svg+xml;
    gzip_disable "MSIE [1-6]\.";
    
    # 浏览器缓存
    location ~* \.(html)$ {
        expires -1;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }
    
    # ==========================================
    # 错误页面（可选）
    # ==========================================
    # error_page 404 /404.html;
    # error_page 500 502 503 504 /50x.html;
}

# ==========================================
# HTTP 到 HTTPS 重定向（可选）
# ==========================================
# 如果需要强制 HTTPS，可以使用单独的 server 块
# server {
#     listen 80;
#     server_name www.example.com;
#     return 301 https://$server_name$request_uri;
# }
