# 🚀 启动所有服务指南

## 📋 服务端口分配

| 服务 | 端口 | 说明 |
|------|------|------|
| frontend-a（用户端） | 3000 | 支付测试用 |
| frontend-b（管理端） | 3001 | 管理后台 |
| frontend-c（卖家端） | 3002 | 核销端 |
| 后端服务 | 8080 | API服务 |
| MySQL | 3307 | 数据库 |
| natapp | - | 内网穿透 |

---

## 🔧 启动步骤

### 1. 启动后端服务（必须）

**方法 1：使用 IDEA**
1. 打开 `shared-backend` 项目
2. 找到主类（带 `@SpringBootApplication` 注解）
3. 右键 → Run

**方法 2：使用命令行**
```bash
cd shared-backend
mvn spring-boot:run
```

**验证**：
```bash
curl http://localhost:8080/api/v1/payment/alipay/notify -X POST
```

如果返回内容（即使是错误），说明后端启动成功。

---

### 2. 重启 frontend-b（管理端）

**停止当前服务**：
- 在运行的终端按 `Ctrl + C`

**重新启动**：
```bash
cd frontend-b
npm run dev
```

**访问**：http://localhost:3001

---

### 3. 启动 frontend-a（用户端）- 支付测试用

```bash
cd frontend-a
npm run dev
```

**访问**：http://localhost:3000

---

### 4. 启动 natapp（支付功能必须）

```bash
natapp -authtoken=你的token
```

**验证**：
```bash
curl http://p22294f8.natappfree.cc
```

---

## ✅ 验证所有服务

运行测试脚本：
```bash
test-natapp-alipay.bat
```

或手动验证：

### 后端服务
```bash
curl http://localhost:8080/api/v1/payment/alipay/notify -X POST
```

### 前端服务
- frontend-a: http://localhost:3000
- frontend-b: http://localhost:3001
- frontend-c: http://localhost:3002

### 内网穿透
```bash
curl http://p22294f8.natappfree.cc
```

---

## 🎯 当前需要做的

### 立即执行：

1. **停止 frontend-b**
   - 在终端按 `Ctrl + C`

2. **启动后端服务**
   ```bash
   cd shared-backend
   mvn spring-boot:run
   ```
   
   等待看到类似输出：
   ```
   Started Application in X.XXX seconds
   ```

3. **重启 frontend-b**
   ```bash
   cd frontend-b
 