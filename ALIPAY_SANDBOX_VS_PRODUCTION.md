# 支付宝沙箱 vs 生产环境 - 完整说明

## 🤔 沙箱的意义是什么？

### 沙箱环境的作用

1. **免费测试**：不需要真实的营业执照和企业认证，个人开发者就可以测试
2. **无需真实支付**：使用沙箱提供的测试账号，不会产生真实的资金流动
3. **快速开发**：可以快速验证支付流程是否正确，不需要等待企业认证审核
4. **调试方便**：可以反复测试，不用担心产生费用或影响真实数据

### 沙箱 vs 生产环境对比

| 项目 | 沙箱环境 | 生产环境 |
|------|---------|---------|
| **使用场景** | 开发、测试 | 正式上线 |
| **认证要求** | 个人账号即可 | 需要企业认证 |
| **费用** | 完全免费 | 需要支付手续费（约0.6%） |
| **支付账号** | 沙箱测试账号 | 真实支付宝账号 |
| **资金流动** | 虚拟资金，不真实扣款 | 真实资金流动 |
| **APPID** | 沙箱专用APPID | 企业应用APPID |
| **网关地址** | `openapi-sandbox.dl.alipaydev.com` | `openapi.alipay.com` |
| **审核** | 无需审核 | 需要审核（1-3个工作日） |

### 典型开发流程

```
1. 开发阶段 → 使用沙箱环境测试
   ↓
2. 测试通过 → 申请企业认证
   ↓
3. 获得认证 → 创建生产应用
   ↓
4. 配置生产环境 → 切换到生产配置
   ↓
5. 正式上线 → 用户真实支付
```

---

## 🏢 生产环境的支付商家是谁？

### 答案：就是你自己的公司/企业

生产环境的支付商家就是**你的公司或企业**，需要：

1. **企业营业执照**
2. **企业支付宝账号**
3. **法人身份证**
4. **对公银行账户**（用于结算）

### 个人开发者怎么办？

如果你是个人开发者，有两个选择：

#### 选项 1：注册个体工商户（推荐）

- 成本低（几百元）
- 流程简单（1-2周）
- 可以申请支付宝企业认证
- 可以使用生产环境支付

#### 选项 2：使用第三方支付服务商

- 使用聚合支付平台（如：Ping++、BeeCloud）
- 他们已经有企业认证
- 你只需要接入他们的 API
- 需要支付额外的服务费

---

## 📝 生产环境配置完整流程

### 第一步：企业认证（前提条件）

#### 1.1 准备材料

- 企业营业执照
- 法人身份证
- 企业支付宝账号
- 对公银行账户信息

#### 1.2 注册企业支付宝账号

1. 访问：https://enterpriseportal.alipay.com/
2. 选择"企业账户注册"
3. 填写企业信息
4. 上传营业执照
5. 完成认证（1-3个工作日）

### 第二步：创建应用

#### 2.1 登录支付宝开放平台

访问：https://open.alipay.com/

#### 2.2 创建应用

1. 点击"控制台" → "网页&移动应用"
2. 点击"创建应用"
3. 填写应用信息：
   - 应用名称：展览门票管理系统
   - 应用图标：上传你的 Logo
   - 应用简介：在线购买展览门票
   - 应用类型：网页应用

#### 2.3 配置应用

1. **配置密钥**：
   - 选择"开发设置" → "接口加签方式"
   - 选择"公钥"模式
   - 生成 RSA2 密钥对（下面有工具）
   - 上传你的公钥
   - 保存支付宝公钥

2. **配置回调地址**：
   - 授权回调地址：`https://www.yourdomain.com`
   - 应用网关：`https://www.yourdomain.com/api/v1/payment/alipay/notify`

3. **签约产品**：
   - 点击"产品绑定"
   - 选择"手机网站支付"
   - 选择"电脑网站支付"
   - 提交签约申请

#### 2.4 等待审核

- 审核时间：1-3个工作日
- 审核通过后会收到短信和邮件通知

### 第三步：生成密钥对

#### 3.1 下载密钥生成工具

访问：https://opendocs.alipay.com/common/02kipl

下载"支付宝开放平台开发助手"

#### 3.2 生成密钥

1. 打开工具
2. 选择"RSA2(SHA256)密钥"
3. 点击"生成密钥"
4. 保存：
   - **应用私钥**（merchant_private_key）- 自己保管，不要泄露
   - **应用公钥** - 上传到支付宝开放平台
5. 从支付宝开放平台获取：
   - **支付宝公钥**（alipay_public_key）

### 第四步：配置代码

#### 4.1 不需要修改代码！

我已经帮你配置好了，只需要修改配置文件即可。

#### 4.2 修改 application.yml

打开 `shared-backend/src/main/resources/application.yml`：

```yaml
spring:
  profiles:
    active: prod  # 改为 prod

---
# 生产环境配置
spring:
  config:
    activate:
      on-profile: prod

alipay:
  # 从支付宝开放平台获取
  app-id: 2021001234567890  # 你的真实 APPID
  
  # 你生成的应用私钥（很长的一串）
  merchant-private-key: MIIEvQIBADANBgkqhkiG9w0BAQE...（完整的私钥）
  
  # 从支付宝开放平台获取的支付宝公钥
  alipay-public-key: MIIBIjANBgkqhkiG9w0BAQEFAAO...（完整的公钥）
  
  # 你的域名
  notify-url: https://www.yourdomain.com/api/v1/payment/alipay/notify
  return-url: https://www.yourdomain.com/order-success
  
  # 生产网关
  gateway-url: https://openapi.alipay.com/gateway.do
```

#### 4.3 使用环境变量（更安全，推荐）

不要把密钥直接写在配置文件中，使用环境变量：

```yaml
alipay:
  app-id: ${ALIPAY_APP_ID}
  merchant-private-key: ${ALIPAY_MERCHANT_PRIVATE_KEY}
  alipay-public-key: ${ALIPAY_PUBLIC_KEY}
  notify-url: https://www.yourdomain.com/api/v1/payment/alipay/notify
  return-url: https://www.yourdomain.com/order-success
  gateway-url: https://openapi.alipay.com/gateway.do
```

在服务器上设置环境变量：

```bash
# 编辑 /etc/profile
sudo vim /etc/profile

# 添加以下内容
export ALIPAY_APP_ID="2021001234567890"
export ALIPAY_MERCHANT_PRIVATE_KEY="MIIEvQIBADANBgkqhkiG9w0BAQE..."
export ALIPAY_PUBLIC_KEY="MIIBIjANBgkqhkiG9w0BAQEFAAO..."

# 使配置生效
source /etc/profile
```

### 第五步：部署到服务器

#### 5.1 打包项目

```bash
cd shared-backend
mvn clean package -DskipTests
```

#### 5.2 上传到服务器

```bash
scp target/buyticket-0.0.1-SNAPSHOT.jar root@your-server-ip:/opt/buyticket/
```

#### 5.3 启动服务

```bash
# 方式 1：直接启动
java -jar /opt/buyticket/buyticket-0.0.1-SNAPSHOT.jar --spring.profiles.active=prod

# 方式 2：使用 nohup 后台运行
nohup java -jar /opt/buyticket/buyticket-0.0.1-SNAPSHOT.jar --spring.profiles.active=prod > /var/log/buyticket/console.log 2>&1 &
```

---

## 🔄 如何在生产环境修改代码？

### 方案 1：本地修改 + 重新部署（推荐）

```bash
# 1. 在本地修改代码
# 2. 提交到 Git
git add .
git commit -m "修复支付问题"
git push

# 3. 在服务器上拉取最新代码
ssh root@your-server-ip
cd /opt/buyticket/source
git pull

# 4. 重新打包
mvn clean package -DskipTests

# 5. 停止旧服务
ps -ef | grep buyticket
kill -9 <PID>

# 6. 启动新服务
nohup java -jar target/buyticket-0.0.1-SNAPSHOT.jar --spring.profiles.active=prod > /var/log/buyticket/console.log 2>&1 &
```

### 方案 2：使用 CI/CD 自动部署（高级）

使用 Jenkins、GitLab CI 或 GitHub Actions 自动部署：

```yaml
# .github/workflows/deploy.yml
name: Deploy to Production

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - name: Set up JDK 17
        uses: actions/setup-java@v2
        with:
          java-version: '17'
      
      - name: Build with Maven
        run: mvn clean package -DskipTests
      
      - name: Deploy to Server
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          source: "target/*.jar"
          target: "/opt/buyticket/"
      
      - name: Restart Service
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            systemctl restart buyticket
```

### 方案 3：热部署（不推荐生产环境）

使用 Spring Boot DevTools，但生产环境不推荐。

---

## 📊 配置对比表

### 开发环境（沙箱）

```yaml
spring:
  profiles:
    active: dev

alipay:
  app-id: 9021000158671506  # 沙箱 APPID
  merchant-private-key: MIIEvQIBADANBgkqhkiG9w0BAQE...  # 沙箱私钥
  alipay-public-key: MIIBIjANBgkqhkiG9w0BAQEFAAO...  # 沙箱公钥
  notify-url: http://abc123.natappfree.cc/api/v1/payment/alipay/notify  # natapp 地址
  return-url: http://localhost:5173/order-success  # 本地地址
  gateway-url: https://openapi-sandbox.dl.alipaydev.com/gateway.do  # 沙箱网关
```

### 生产环境

```yaml
spring:
  profiles:
    active: prod

alipay:
  app-id: 2021001234567890  # 真实 APPID
  merchant-private-key: ${ALIPAY_MERCHANT_PRIVATE_KEY}  # 真实私钥（环境变量）
  alipay-public-key: ${ALIPAY_PUBLIC_KEY}  # 真实公钥（环境变量）
  notify-url: https://www.yourdomain.com/api/v1/payment/alipay/notify  # 真实域名
  return-url: https://www.yourdomain.com/order-success  # 真实域名
  gateway-url: https://openapi.alipay.com/gateway.do  # 生产网关
```

---

## ✅ 切换环境的步骤

### 从沙箱切换到生产

1. **获取生产环境配置**：
   - 完成企业认证
   - 创建应用并审核通过
   - 生成密钥对
   - 获取 APPID、私钥、公钥

2. **修改配置文件**：
   ```yaml
   spring:
     profiles:
       active: prod  # 改这一行
   ```

3. **配置生产环境参数**：
   - 修改 `application.yml` 中的 prod 配置
   - 或者设置环境变量

4. **重新打包部署**：
   ```bash
   mvn clean package -DskipTests
   ```

5. **部署到服务器**：
   ```bash
   java -jar buyticket.jar --spring.profiles.active=prod
   ```

### 从生产切换回沙箱（用于测试）

只需要改一行：

```yaml
spring:
  profiles:
    active: dev  # 改回 dev
```

---

## 🎯 总结

### 沙箱的意义

1. ✅ **免费测试**：不需要企业认证，个人就能测试
2. ✅ **快速开发**：可以快速验证支付流程
3. ✅ **无风险**：不会产生真实的资金流动
4. ✅ **方便调试**：可以反复测试

### 生产环境配置

1. **商家**：你自己的公司/企业
2. **要求**：企业认证 + 营业执照
3. **配置**：只需修改 `application.yml`，不需要改代码
4. **部署**：打包后上传到服务器

### 代码修改

1. **本地修改**：在本地开发环境修改代码
2. **提交代码**：提交到 Git
3. **服务器部署**：拉取代码 → 打包 → 重启服务

### 关键点

- ✅ 代码已经支持环境切换，不需要修改代码
- ✅ 只需要修改 `application.yml` 中的 `spring.profiles.active`
- ✅ 生产环境使用环境变量存储密钥更安全
- ✅ 沙箱用于开发测试，生产用于正式上线

---

## 📞 常见问题

### Q1: 我是个人开发者，能用生产环境吗？

**答**：需要注册个体工商户，或者使用第三方支付服务商。

### Q2: 沙箱测试通过了，生产环境还需要测试吗？

**答**：需要！生产环境要用小额真实支付测试一次。

### Q3: 生产环境的手续费是多少？

**答**：支付宝手续费约 0.6%，具体以签约时的费率为准。

### Q4: 如何查看生产环境的交易记录？

**答**：登录支付宝商家中心：https://b.alipay.com/

### Q5: 代码需要改吗？

**答**：不需要！只需要修改配置文件 `application.yml`。

---

希望这个文档能帮你理解沙箱和生产环境的关系！🎉
